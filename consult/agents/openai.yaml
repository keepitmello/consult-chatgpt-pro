interface:
  display_name: "Consult"
  short_description: "Get a verified ChatGPT Work-project second opinion through Aside"
  default_prompt: "Use $consult only with an explicit --quality xhigh or --quality pro flag; submit through the deterministic Aside REPL runner within 120 seconds and verify the returned advice locally."
policy:
  allow_implicit_invocation: true
