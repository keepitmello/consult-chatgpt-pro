interface:
  display_name: "Consult"
  short_description: "Get a verified ChatGPT Work-project second opinion through Aside"
  default_prompt: "Use $consult to prepare a focused packet, delegate the browser workflow to Aside, and verify the returned advice locally."
policy:
  allow_implicit_invocation: true
